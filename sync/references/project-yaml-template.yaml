version: 1

project:
  name: ""
  root: "."

paths:
  configs_dir: "configs"
  output_current: "output/current"
  output_archived_runs: "output/archived_runs"
  analysis_log: "notes/analysis_log.md"
  manuscript_log: "notes/manuscript_log.md"
  manuscript_current_dir: "paper/manuscript/current"
  manuscript_versions_dir: "paper/manuscript/versions"
  supplement_current_dir: "paper/supplement/current"
  supplement_versions_dir: "paper/supplement/versions"
  response_to_reviewers_dir: "paper/response_to_reviewers"

compatibility:
  optional_only: true
  preserve_other_skill_state: true
  note: "Use this file only when the project needs non-default paths."
