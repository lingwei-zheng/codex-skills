interface:
  display_name: "Handoff"
  short_description: "Compact this conversation for a fresh agent."
  default_prompt: "Use $handoff to write a concise temporary handoff for the next agent, referencing existing artifacts and listing the next action and suggested skills."
policy:
  allow_implicit_invocation: false
