interface:
  display_name: "Engineering Figure Agent"
  short_description: "Execute reviewed schematics with OpenAI or Draw.io."
  default_prompt: "Use $engineering-figure-agent to execute my reviewed figure brief with an OpenAI image or editable Draw.io backend while preserving its semantic contract."

policy:
  allow_implicit_invocation: true
